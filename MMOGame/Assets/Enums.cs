using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums : MonoBehaviour
{
    public enum enemyState { wander, attacking};
    public enum selected {notselected, targeted};
}
